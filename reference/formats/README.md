# Format notes

One file per on-disk format, verified against the original 1995 game files. Each
note ties its claims to evidence: the offsets, sizes, or disassembly that prove
them. Anything still unconfirmed is marked as such.

The format details come from the game's original developer. Where that account
and the bytes disagree, the bytes win, and the note says so.
